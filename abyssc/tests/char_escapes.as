i32 main(i32* argc, str* argv) {
    char newline = '\n';
    char tab = '\t';
    char quote = '\'';
    char slash = '\\';
    char zero = '\0';
    return (i32)newline + (i32)tab + (i32)quote + (i32)slash + (i32)zero;
}
