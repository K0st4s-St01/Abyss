import meta;

struct Box {
    i32 value;

    i32 read() {
        return value;
    }
}

i32 main(i32* argc, str* argv) {
    Count n = 5;
    Point p = {2, 3};
    Box b = {4};
    Readable* r = &b;
    if (r) {
        return n + Red + p.x + imported_counter;
    }
    return 1;
}
